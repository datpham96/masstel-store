const initialState = {};

const homeReducers = (state = initialState, action) => {
  switch (action.type) {
    case action.type:
      return {};
    default:
      return state;
  }
};

export default homeReducers;
